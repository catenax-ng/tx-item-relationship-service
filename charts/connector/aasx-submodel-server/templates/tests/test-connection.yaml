apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aasx-submodel-server.fullname" . }}-test-connection"
  labels:
    {{- include "aasx-submodel-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aasx-submodel-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
