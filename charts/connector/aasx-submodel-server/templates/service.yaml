apiVersion: v1
kind: Service
metadata:
  name: {{ include "aasx-submodel-server.fullname" . }}
  labels:
    {{- include "aasx-submodel-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aasx-submodel-server.selectorLabels" . | nindent 4 }}
